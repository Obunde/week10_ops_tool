"""week10_ops_tool — Data Freshness / SLA Monitor.

Core package: configuration loading, freshness checks, report rendering and
alert delivery. The CLI lives in ``main.py`` and the Streamlit UI in ``app.py``.
"""

__version__ = "1.0.0"
